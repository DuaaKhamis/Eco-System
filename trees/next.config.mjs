/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['encrypted-tbn0.gstatic.com', 'images.unsplash.com'], // Add any other domains you might use
    },
  }
  
  export default nextConfig;